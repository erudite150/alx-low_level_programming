#include <stdio.h>

/**
 * main - Prints all possible combinations of single-digit numbers
 *
 * Return: Always 0
 */
int main(void)
{
	int cha;
	for (cha = 48; cha <= 57; cha++)
	{

		putchar(cha);
		if (cha != 57);
		{
		putchar(44);
		putchar(32);
	}
	}
	
	putchar(10); 

	return (0);
}
